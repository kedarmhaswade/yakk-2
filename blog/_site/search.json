[
  {
    "objectID": "index.html",
    "href": "index.html",
    "title": "Welcome to an Artificial Blog",
    "section": "",
    "text": "I maintain a blog here. The blog posts pertain to my research interests.\nEach blog post is a Jupyter notebook.\n\n\n\nOn Compilers and Interpreters (30 November 2025)\n\n\n\n\nHello, World!"
  },
  {
    "objectID": "compilers-interpreters.html",
    "href": "compilers-interpreters.html",
    "title": "On Compilers and Interpreters (30 November 2025)",
    "section": "",
    "text": "I wrote this quick blog-post while reviewing Python Functions.\nI stumbled upon the following statements as I read the Functions section in Python In a Nutshell by Martelli et.al. (with a few changes for context):\n\nWhen the Python interpreter (the python executable on your computer) encounters a def statement in your Python script or REPL, it compiles the function body, creating a function object.\n\nThis statement subtly describes the dual nature of python (the name of a program in an executable form), which is both a compiler and an interpreter. It also prompted me to reexamine compilation and interpretation in the context of programming languages. I had always encountered these terms. I simply asked myself: Do I understand these terms and the difference between them now?\nThis blog-post answers that question.\nFirst, let’s look up the contemporary dictionary meanings of the two verbs: compile and interpret. I have stated the meanings most relevant in a computing context in boldface.\n\nCompile (v.): 1) get or gather together (synonym: to accumulate), 2) put together out of existing material (e.g. compile a list), or 3) use a computer program (a “compiler”) to translate source code written in a particular programming language into computer-readable machine code that can be executed.\nInterpret (v.): 1) make sense of; assign meaning to (e.g. interpret a message) 2) give rendition of (e.g. a Beethoven sonata) 3) create an image or likeness of (synonym: to represent) 3) restate words from one language into another language (synonym: to translate), or 4) make sense of a language.\n\nAren’t the relevant definitions confusing? Don’t they mean the same thing (to translate)? Restating (modified slightly to suit Python, although it doesn’t matter) them:\n\nCompile: Use the python executable to translate a complete Python program into the Intel microprocessor’s machine code (instructions) which can be later run or executed.\nInterpret: Use the python executable to translate or restate the words from Python (e.g. an assignment like x = 20) into another language like Intel microprocessor’s (x86 64) machine code (something like 48 c7 c0 01 00 00 00 encoded in binary) and run it, so that actual physical changes occur at the semiconductor level! This is like how a human interpreter interprets a foreign president’s response for the native speakers to understand.\n\nWhen a program runs on a computer, the CPU simply executes corresponding instructions one by one. A runnable program is a file with 0s and 1s (binary). It is also called an executable. The executable then contains a sequence of machine code instructions.\nThus, the CPU is the ultimate interpreter or executor which runs instructions in its machine code corresponding to any program. The CPU does not translate the program written in computer’s RAM in a ‘language’ like 48 c7 c0 01 00 00 00 into some other language, but it, like a faithful and unerring servant, “directly makes sense of it” by taking actions in the hardware (thus interpreting it–See meaning #1 of interpret above).\nFor our program, written in any language other than the machine code, to do anything on a physical machine, therefore, we need two things:\n\nOur program’s code somehow translated into a binary machine code (instructions for a CPU).\nA CPU that interprets or makes sense of it by taking the instructed actions.\n\nWe now have more nuanced definitions:\n\nCompile: Translate something from one formal language into another. Formal languages, unlike natural languages, have unambiguous grammars.\nInterpret: Make sense of (well-defined) instructions in a language by taking actions intended therein.\n\nUntil about the 70s (and of course even now), we had a programming language like C in which programmers wrote their programs, a compiler like the gcc (which is itself a big program written in C and compiled into a computer’s machine code) that compiled those C programs into a computer’s machine code, and a machine that interpreted that machine code. A typical workflow of an application programmer was straightforward:\n\nCreate a C program file, a.c. This file is not executable. It contains the C source code.\nCompile it: For example, gcc a.c. This creates an executable file a.out which has all the machine instructions. The compiler ensures that a.c is valid C code, and a.out is valid machine code.\nRun the executable file: ./a.out which transfers (loads) the instructions in computer’s memory. We didn’t call this step interpretation, although the computer makes sense of (interprets) those instructions.\n\nThis required that different computers had different compilers, although the C program file was the same. You could give your little C program, a.c (not a.out, because it was for your computer), to your friend on a floppy, and s/he would compile it on their computer (which may be different from yours) to, say, a.exe, and run it.\nWhat is a compiler like gcc then? Clearly, it must be a program, an executable file that has all the instructions for the machine. When run, its job is to take your C program and produce a corresponding executable. There is of course a bootstrapping problem here if gcc were not available as a sequence of machine instructions. If gcc was the name of the executable program that was created by compiling a C (or other language) source program, then we must compile that source program somehow! For now, let’s assume that gcc exists.\nLater, perhaps in the early 90s, the idea of a writing computer programs for a virtual machine came along. We’ll not go into the benefits of this idea here.\nThe advent of virtual machines made the term ‘interpreter’ more well-known. Now, a compiler translated programs from a source language into an instruction language, not for any physical machine, but for a virtual machine. The instructions for the virtual machine were called the bytecode.\nA compiler for the virtual machine creates its bytecode from a program in another language.\nAn interpreter interprets (makes sense of) the bytecode by transparently translating it into the machine code of a given physical computer.\nTwo notable programming languages developed with a virtual machine in mind are Python and Java. Let’s consider Java first. An application programmer’s workflow now becomes slightly more complicated:\n\nCreate a Java program file, A.java. This file is not executable. It contains the Java source code.\nCompile A.java using another executable program on your computer, javac: This creates another file, called the Java class file, named A.class, which has all the bytecode (instructions) for the Java virtual machine. A.class can not be executed (or run) on any physical computer all by itself.\nRun A.class by using yet another executable program on your computer, java: This does not create any file. The java program simply interprets the bytecode in A.class by transparently translating it into the machine code for a given physical computer (CPU), which executes it. The fact remains. CPU is the ultimate interpreter. However, there is no separate step, “running the executable file”.\n\nFor Java, javac is the compiler of your Java program, java is the interpreter of the corresponding bytecode. The bytecode is transparently translated into the machine code. CPU is the ultimate interpreter of the machine code.\nFor Python, python is both the compiler and the interpreter. The two distinct functions are merged into one executable program that compiles and interprets your Python program. The bytecode is transparently translated into the machine code. CPU is the ultimate interpreter of the machine code.\nConsider a simple Python program:\n1 def square(n):\n2    return n*n\n\n3 s = square(5)\n4 print(s)\nWhen the python executable encounters a function, the def statement (line 1), it first compiles the function named square to the bytecode (remember, python is both a compiler and an interpreter) for the Python virtual machine. A subsequent call to that function (via its bytecode corresponding to the line 3) tells the python interpreter to interpret the function’s bytecode compiled previously.\nThe python interpreter is a big program written in C, compiled by someone else. You run it on a given computer (just like you ran a.out compiled from your a.c above). Its source code (at a high-level) looks like this:\nfor (;;) {\n    switch (opcode) {\n        /* cases refer to actual bytecodes in a hidden file */\n        case LOAD_CONST: ...\n        case CALL: ...\n        case RETURN_VALUE: ...\n        ...\n    }\n}\nThe situation is subtly different when your code calls a Python builtin function like print (line 4) or sum: Unlike square, there is no bytecode available for print. Instead, the interpreter has access to its machine code, which was previously compiled. python interpreter then cedes the control to the CPU which interprets that machine code.\nSee this answer on Computer Science StackExchange for more details."
  },
  {
    "objectID": "say-hi.html",
    "href": "say-hi.html",
    "title": "Hello, World!",
    "section": "",
    "text": "This is my first blog post, powered by Quarto and GitHub Workflow. I am not exactly happy because it took a half day to set all this up. But now that it is all set up, I can hopefully produce some readable blog-posts that are version-controlled."
  },
  {
    "objectID": "say-hi.html#my-workflow",
    "href": "say-hi.html#my-workflow",
    "title": "Hello, World!",
    "section": "My Workflow",
    "text": "My Workflow\n\nKeep my phase-wise notebooks (which are expected to be detailed explorations) up-to-date. I decided not to convert them into a website like this one. Instead, they are just executed notebooks (even with errors to demonstrate something) committed to my GitHub repo.\nJust see the individual notebooks in phase-1, phase-2 etc. I link my all my phase-star notebooks from my blog using the GH source links. A typical link is https://github.com/kedarmhaswade/yakk-2/blob/main/phase-1/day10_pretest.ipynb and it should go verbatim in a blog-post.\nThat is my shortcut to host the notebooks without any explicit hosting overhead! I think it works for me.\nWhen I want to write a blog-post, I come to this folder (/blog of my repository) and create my blog-post also as a notebook (contains mostly Markdown, but may contain some exploration code-blocks too).\nI ask quarto to render the static HTML from these “notebooks as blog-posts”. For the same purpose, I utilize a GitHub workflow for static HTML.\nAfter I am done with my blog-post, I render the Quarto website locally and make sure it succeeds and generates the static pages in the _site subfolder of my blog folder.\nOn commit and push, GitHub runs my workflow and pushes the static HTML (my blog-posts) to this website that you are reading it on.\nSimple, eh?"
  }
]